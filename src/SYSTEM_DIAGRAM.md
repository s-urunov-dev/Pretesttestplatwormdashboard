# 🏗️ System Architecture & Error Flow

## Current System State

```
┌─────────────────────────────────────────────────────────────────────┐
│                          IELTS Admin Panel                          │
│                     (Frontend - React + TypeScript)                 │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ API Request
                                  ↓
        ┌────────────────────────────────────────────────┐
        │   GET /api/v1/readings/1/passages/            │
        └────────────────────────────────────────────────┘
                                  │
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      Backend API (Django REST)                      │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │  ReadingListDetailAPIView                               │     │
│   │    ↓                                                     │     │
│   │  ReadingPassageModelSerializer                          │     │
│   │    ↓                                                     │     │
│   │  QuestionGroupModelSerializer                           │     │
│   │    ↓                                                     │     │
│   │  MatchingStatementSerializer  ← ❌ ERROR HERE           │     │
│   │    ↓                                                     │     │
│   │  get_questions(obj)                                     │     │
│   │    ↓                                                     │     │
│   │  obj.statement  ← obj is RelatedManager, not instance  │     │
│   └─────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Returns HTML Error Page
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    Frontend Error Handling                          │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │  lib/api-cleaned.ts                                     │     │
│   │    ↓                                                     │     │
│   │  Detects HTML response (<!DOCTYPE html>)                │     │
│   │    ↓                                                     │     │
│   │  Extracts error type from <title>                       │     │
│   │    ↓                                                     │     │
│   │  Throws user-friendly error                             │     │
│   └─────────────────────────────────────────────────────────┘     │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │  pages/AddQuestionPage.tsx                              │     │
│   │    ↓                                                     │     │
│   │  Catches error in loadPassages()                        │     │
│   │    ↓                                                     │     │
│   │  Sets passagesError state                               │     │
│   │    ↓                                                     │     │
│   │  Renders BackendErrorAlert component                    │     │
│   └─────────────────────────────────────────────────────────┘     │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │  components/BackendErrorAlert.tsx                       │     │
│   │    ↓                                                     │     │
│   │  ✅ Shows beautiful error UI                            │     │
│   │  ✅ Displays fix instructions                           │     │
│   │  ✅ Links to documentation                              │     │
│   │  ✅ Provides retry button                               │     │
│   └─────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ↓
                    ┌──────────────────────────────┐
                    │   User sees error alert:     │
                    │                              │
                    │   "Backend serializer error" │
                    │   "Check documentation"      │
                    │   [Retry Button]             │
                    └──────────────────────────────┘
```

---

## Error Root Cause

```
┌─────────────────────────────────────────────────────────────────────┐
│                     Django Model Relationships                      │
└─────────────────────────────────────────────────────────────────────┘

   QuestionGroup Model               MatchingStatement Model
   ┌────────────────┐               ┌──────────────────────┐
   │ id              │               │ id                   │
   │ from_value      │               │ statement (array)    │
   │ to_value        │               │ option (array)       │
   │ ...             │◄─────────────┤ question_group (FK)  │
   └────────────────┘               └──────────────────────┘
           │
           │ Reverse Relation
           ↓
   matching (RelatedManager)  ← ❌ Serializer accesses this directly


┌─────────────────────────────────────────────────────────────────────┐
│                    What Serializer Expects vs Gets                  │
└─────────────────────────────────────────────────────────────────────┘

Expected:
    obj = MatchingStatement instance
    obj.statement → ['Question 1', 'Question 2']  ✅
    obj.option → [['A', 'B'], ['C', 'D']]  ✅

Actually Gets:
    obj = RelatedManager (Django's reverse relation manager)
    obj.statement → AttributeError ❌
    obj.option → AttributeError ❌
```

---

## Solution Flow

### Option 1: Add source Parameter (Recommended)

```
QuestionGroupModelSerializer
    ↓
matching = MatchingStatementSerializer(
    source='matchingstatement',  ← Tells Django which field to use
    required=False,
    read_only=True
)
    ↓
Django automatically resolves to correct instance
    ↓
obj = MatchingStatement instance ✅
    ↓
obj.statement works! ✅
```

### Option 2: Handle RelatedManager in Method

```
get_questions(obj)
    ↓
Check if obj is RelatedManager
    ↓
If yes: obj = obj.first()  (get first instance)
    ↓
If no: use obj directly
    ↓
Safe attribute access with hasattr()
    ↓
Return questions ✅
```

---

## After Fix Applied

```
┌─────────────────────────────────────────────────────────────────────┐
│                          IELTS Admin Panel                          │
│                     (Frontend - React + TypeScript)                 │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ API Request
                                  ↓
        ┌────────────────────────────────────────────────┐
        │   GET /api/v1/readings/1/passages/            │
        └────────────────────────────────────────────────┘
                                  │
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      Backend API (Django REST)                      │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │  ReadingListDetailAPIView                               │     │
│   │    ↓                                                     │     │
│   │  ReadingPassageModelSerializer                          │     │
│   │    ↓                                                     │     │
│   │  QuestionGroupModelSerializer                           │     │
│   │    ↓                                                     │     │
│   │  MatchingStatementSerializer (source='...')  ✅         │     │
│   │    ↓                                                     │     │
│   │  get_questions(obj)  ← obj is now correct instance ✅  │     │
│   │    ↓                                                     │     │
│   │  obj.statement works! ✅                                │     │
│   └─────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ Returns JSON ✅
                                  ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         Frontend Receives Data                      │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │  lib/api-cleaned.ts                                     │     │
│   │    ↓                                                     │     │
│   │  response.ok = true ✅                                  │     │
│   │    ↓                                                     │     │
│   │  Returns parsed JSON data                               │     │
│   └─────────────────────────────────────────────────────────┘     │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────┐     │
│   │  pages/AddQuestionPage.tsx                              │     │
│   │    ↓                                                     │     │
│   │  setPassages(response.results) ✅                       │     │
│   │    ↓                                                     │     │
│   │  Renders passage form with data ✅                      │     │
│   └─────────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ↓
                    ┌──────────────────────────────┐
                    │   User sees:                 │
                    │                              │
                    │   ✅ Passage title loaded    │
                    │   ✅ Passage body loaded     │
                    │   ✅ Question groups loaded  │
                    │   ✅ Can edit/save           │
                    └──────────────────────────────┘
```

---

## Documentation Structure

```
Project Root
│
├── 📄 START_HERE.md ← 👈 Begin here!
│   └── Links to all other docs
│
├── 📊 SYSTEM_DIAGRAM.md (this file)
│   └── Visual architecture & flow
│
├── 🎯 BACKEND_FIX_VISUAL.md
│   └── ASCII art guide (Uzbek)
│
├── ⚡ BACKEND_FIX_QUICK_GUIDE.md
│   └── Quick 3-minute fix (Uzbek)
│
├── 📖 URGENT_BACKEND_FIX.md
│   └── Detailed troubleshooting (English)
│
├── 📋 README_BACKEND_ERROR.md
│   └── Overview (Bilingual)
│
└── 📝 IMPLEMENTATION_SUMMARY.md
    └── Technical details (English)
```

---

## Component Interaction

```
┌────────────────────────────────────────────────────────────────┐
│                    AddQuestionPage Component                   │
│                                                                │
│  State:                                                        │
│  ├─ loadingPassages: boolean                                  │
│  ├─ passagesError: Error | null  ← New!                       │
│  └─ passages: any[]                                            │
│                                                                │
│  Functions:                                                    │
│  └─ loadPassages() → getReadingPassages() → API               │
│                                                                │
│  Render:                                                       │
│  ├─ {loadingPassages && <Loader />}                           │
│  ├─ {passagesError && <BackendErrorAlert />}  ← New!          │
│  └─ {!loadingPassages && !passagesError && <Form />}          │
└────────────────────────────────────────────────────────────────┘
                             │
                             ↓
            ┌────────────────────────────────┐
            │   BackendErrorAlert Component  │
            │                                │
            │   Props:                       │
            │   ├─ error: Error              │
            │   └─ onRetry?: () => void      │
            │                                │
            │   Features:                    │
            │   ├─ Professional UI           │
            │   ├─ Fix instructions          │
            │   ├─ Documentation links       │
            │   └─ Retry button              │
            └────────────────────────────────┘
```

---

## API Flow

```
Frontend                    Backend                     Database
   │                           │                            │
   │  GET /readings/1/        │                            │
   │  passages/                │                            │
   ├──────────────────────────>│                            │
   │                           │                            │
   │                           │  SELECT * FROM passages    │
   │                           │  WHERE reading_id = 1      │
   │                           ├───────────────────────────>│
   │                           │                            │
   │                           │  SELECT * FROM groups      │
   │                           ├───────────────────────────>│
   │                           │                            │
   │                           │  ❌ ERROR in serializer    │
   │                           │  (trying to access         │
   │                           │   RelatedManager.statement)│
   │                           │                            │
   │  ← HTML Error Page (500)  │                            │
   │<──────────────────────────┤                            │
   │                           │                            │
   │  Detects HTML             │                            │
   │  Shows BackendErrorAlert  │                            │
   │                           │                            │
```

### After Fix:

```
Frontend                    Backend                     Database
   │                           │                            │
   │  GET /readings/1/        │                            │
   │  passages/                │                            │
   ├──────────────────────────>│                            │
   │                           │                            │
   │                           │  SELECT * FROM passages    │
   │                           ├───────────────────────────>│
   │                           │                            │
   │                           │  SELECT * FROM groups      │
   │                           ├───────────────────────────>│
   │                           │                            │
   │                           │  SELECT * FROM matching    │
   │                           ├───────────────────────────>│
   │                           │                            │
   │                           │  ✅ Serializer works!      │
   │                           │  Returns proper JSON       │
   │                           │                            │
   │  ← JSON Response (200)    │                            │
   │<──────────────────────────┤                            │
   │                           │                            │
   │  Parses data              │                            │
   │  Renders form ✅          │                            │
   │                           │                            │
```

---

## Success Metrics

```
┌────────────────────────────────────────────────────────────┐
│                      Before Fix                            │
├────────────────────────────────────────────────────────────┤
│  ❌ HTML error page returned                               │
│  ❌ Frontend shows blank/loading forever                   │
│  ❌ Console full of errors                                 │
│  ❌ User confused                                           │
│  ❌ No guidance for developers                             │
└────────────────────────────────────────────────────────────┘

                            Fix Applied
                                 ↓

┌────────────────────────────────────────────────────────────┐
│                 After Fix (With Error Handling)            │
├────────────────────────────────────────────────────────────┤
│  ✅ JSON response returned                                 │
│  ✅ Data parsed correctly                                  │
│  ✅ Form rendered with passage data                        │
│  ✅ User can edit and save                                 │
│  ✅ Professional experience maintained                     │
│                                                            │
│  Even if error occurs:                                     │
│  ✅ Beautiful error UI shown                               │
│  ✅ Clear fix instructions provided                        │
│  ✅ User understands it's backend issue                    │
│  ✅ Developer has clear path forward                       │
└────────────────────────────────────────────────────────────┘
```

---

**This diagram shows the complete system architecture and error flow.**

For implementation details, see: [IMPLEMENTATION_SUMMARY.md](./IMPLEMENTATION_SUMMARY.md)  
For quick fix, see: [START_HERE.md](./START_HERE.md)
